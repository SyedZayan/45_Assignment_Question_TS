function make_shirt(size, message) {
    console.log(`The T-shirt size is "${size.toUpperCase()}" and it has the following message "${message}"`);
}
make_shirt("L", "It Should be delivered on time");
export {};
