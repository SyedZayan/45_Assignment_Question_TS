function make_shirt(size: string, message: string) {
  console.log(
    `The T-shirt size is "${size.toUpperCase()}" and it has the following message "${message}"`
  );
}
make_shirt("L", "It Should be delivered on time");
