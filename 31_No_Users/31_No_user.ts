let userNames: string[] = ["Admin", "Hasan", "Ali", "Huzaifa", "Asim"];
if (userNames.length === 0) {
  console.log(" we need to find some users!");
} else {
  userNames = [];
  console.log(
    "All user Names have been removed,And the current length is " +
      userNames.length
  );
}
