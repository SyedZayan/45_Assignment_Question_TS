let userNames = ["Admin", "Hasan", "Ali", "Huzaifa", "Asim"];
for (let i = 1; i <= 4; i++) {
    if (userNames[i] === "Admin") {
        console.log("Hello " + userNames[i] + ", would you like to see a status report?");
    }
    console.log("Hello " + userNames[i] + ",would you like to see a status report?");
}
export {};
